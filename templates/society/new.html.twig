{% extends 'layout.html.twig' %}

{% set current_navigation_id = 'societies' %}
{% block title %}Society - New{% endblock %}

{% block body %}

<div class="row">
    <h3>New Society</h3>
    <form action="{{ path("acts_camdram_society_post") }}" method="post">
        {{ form_widget(form) }}
        <div class="text-right">
            <input type="submit" value="Create" class="button"/>
        </div>
    </form>
</div>

{% endblock %}
