{% extends 'layout.html.twig' %}

{% set current_navigation_id = 'events' %}
{% block title %}Event - {{ form.vars.value.name }} - Edit{% endblock %}

{% block body %}

{{ render(admin_panel(form.vars.value)) }}

<h3>Edit Event</h3>
{{ form_start(form, {'action': path("acts_camdram_event_put", {identifier: form.vars.value.id} )}) }}

{% include 'event/form.html.twig' %}

<div class="text-right">
    <input type="submit" value="Save" class="button"/>
</div>

{{ form_end(form) }}

{% endblock %}
