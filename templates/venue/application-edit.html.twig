{% extends 'layout.html.twig' %}

{% set current_navigation_id = 'vacancies' %}
{% block title %}Edit Show Applications{% endblock %}

{% block body %}

{% set current_subnav_id = 'edit_application' %}
{{ render(controller('ActsCamdramBundle:Venue:adminPanel', {org: form.vars.value.venue})) }}

<div class="split-header">
    <h3>Edit show applications advertisement</h3>
</div>

<div class="panel">
    <p>Use the form below to modify or extend your show applications advertisement. The advert will be displayed
    until the expiry date.</p>
    <p>
        Please ensure you have your team members' permission <em>before</em> adding their contact
        details to the advert!
    </p>
</div>

<form action="{{ path("acts_camdram_venue_putadvert", {identifier: org.slug, advertId: advert.id}) }}" method="post">
    {{ include('advert/form.html.twig') }}
</form>

{% endblock %}
