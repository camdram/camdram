{% extends 'layout.html.twig' %}

{% block title %}User - New{% endblock %}

{% block body %}

<div class="row">
    <h3>New User</h3>
    <form action="{{ path("post_user") }}" method="post" class="custom">
        {{ form_widget(form) }}
        <div class="text-right">
            <input type="submit" value="Create" class="button"/>
        </div>
    </form>
</div>

{% endblock %}
