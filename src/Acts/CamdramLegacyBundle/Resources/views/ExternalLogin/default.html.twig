{% extends 'layout.html.twig' %}

{% block title %}External login{% endblock %}

{% block body %}
<h4>Login to Camdram</h4>
{% if site %}
    <p>
    The website <em>"{{ site.name }}"</em> is requesting for you to login using your Camdram account, however it is doing so using a deprecated interface which will soon no longer be supported.
    </p>
    <p>
    You should contact the site owner and ask them to upgrade their application as soon as possible.
    </p>
    <a href="{{ path('acts_camdram_externallogin_auth', {'redirect': redirect}) }}" class="button">Login</a>
{% else %}
    <p class="error">
    Invalid external login attempt. Please <a href="{{ path('acts_camdram_contact_us') }}">
    contact us</a> if this issue persists.
    </p>
{% endif %}
{% endblock %}
