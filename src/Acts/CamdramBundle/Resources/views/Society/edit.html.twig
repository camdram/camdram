{% extends 'ActsCamdramBundle::layout.html.twig' %}

{% set current_navigation_id = 'societies' %}
{% block title %}Society - {{ form.vars.value.name }} - Edit{% endblock %}

{% block body %}

<div class="row">
    <h3>Edit Society</h3>
    <form action="{{ path("put_society", {identifier: form.vars.value.slug, _method: 'put'}) }}" method="post">
        {{ form_widget(form) }}
        <div class="text-right">
            <input type="submit" value="Save" class="button"/>
        </div>
    </form>
</div>

{% endblock %}
