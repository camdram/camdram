{% extends 'ActsCamdramBundle::layout.html.twig' %}

{% block title %}Email Builder - New{% endblock %}

{% block body %}

<div class="row">
    <h3>New Email Builder</h3>
    <form action="{{ path("post_emailbuilder") }}" method="post" class="custom">
        {{ form_widget(form) }}
        <div class="text-right">
            <input type="submit" value="Create" class="button"/>
        </div>
    </form>
</div>

{% endblock %}
