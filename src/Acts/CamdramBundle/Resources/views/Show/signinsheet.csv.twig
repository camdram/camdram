{% set title = 'Sign In sheet for ' ~ show.name ~ ' (' ~ events[0].date|date('j/n/Y') ~ ' - ' ~ events[events|length -1].date|date('j/n/Y') ~ ') - generated by Camdram' %}
{% set blank_cells = '' %}
{%- for i in 0..events|length + 1 -%}
    {% set blank_cells = blank_cells ~ ',,' %}
{%- endfor -%}
{{ title }},
,Tech,,Dress,,
{%- for event in events -%}
    {{ event['date']|date(date_format) }} {{ event.time|date('G:i') }},,
{%- endfor -%},
,{% for i in 0..events|length + 1 %}In,Out,{% endfor %},
Cast,
{% for role in show.getRolesByType('cast') %}
{{ role.person.name }},
{% endfor %},
{%- if show.getRolesByType('band')|length > 0 -%}
,
Band,
{% for role in show.getRolesByType('band') %}
{{ role.person.name }},
{% endfor %},
{%- endif -%}
,
Production Team,
{% for role in show.getRolesByType('prod') %}
{{ role.person.name }},
{% endfor %},
{# space for extra entries, e.g. Counterweight ops #}
{% for i in 0..2 %}
,
{% endfor %},
