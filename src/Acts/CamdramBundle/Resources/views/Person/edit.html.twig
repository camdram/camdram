{% extends 'ActsCamdramBundle::layout.html.twig' %}

{% block title %}Person - {{ form.vars.value.name }} - Edit{% endblock %}

{% block body %}

<div class="row">
    <h3>Edit Person</h3>
    <form action="{{ path("put_person", {slug: form.vars.value.slug, _method: 'put'}) }}" method="post" class="custom">
        {{ form_widget(form) }}
        <div class="text-right">
            <input type="submit" value="Save" class="button"/>
        </div>
    </form>
</div>

{% endblock %}
