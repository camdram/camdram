language: php
matrix:
  include:
    - php: 7.0
    - php: 7.2
  fast_finish: true
cache:
  directories:
  - $HOME/.composer
services:
  - elasticsearch
  - mysql
before_script:
- cp app/config/parameters.travis.yml app/config/parameters.yml
- composer selfupdate
- composer install --no-interaction
script:
- "./runtests"
- "./check_dev_setup"